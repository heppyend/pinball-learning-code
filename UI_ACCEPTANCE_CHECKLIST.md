# 客户端 UI 验收清单（2026-09-21）

> 给负责人人工 Play 验收用。每条都写了**步骤 / 预期 / 失败时请回传什么**。
> 自动化侧已过的部分见文末"已自动验证基线"——**自动验证不能替代你的人工确认**。
> 场景：`Assets/Client/Scenes/ClientShell.unity`（Unity `2022.3.57f1c2`，点 Play）。
> 备份：本清单涉及的所有场景写入前均已备份（`Logs/ClientShell.before-*.unity.bak`）。

---

## A. 个人中心（ProfilePage）—— 本轮重点

| # | 步骤 | 预期 | 失败请回传 |
|---|---|---|---|
| A1 | 大厅点 `ProfileButton` 进个人中心 | 页面打开，**无报错** | Console 首条 Error/Exception |
| A2 | 看**玩家名 / 玩家ID / 战力** | 三处**都有值**（名称、`玩家ID: …`、战力数字） | 哪一处空白 + 截图 |
| A3 | 切到「个性化」页签，看 5 个列表 | **头像 1 / 徽章 1 / 铭牌 1 / 头像框 3 / 称号 1** 项 | 哪个列表条数不对 |
| A4 | 点一个**未拥有**的收藏品 | 弹 **Toast 提示**「尚未拥有：…」 | 有没有提示 |
| A5 | 点一个**已拥有**的收藏品 | 弹 Toast「已使用：…」，该条目出现"使用中"标记 | 有没有切换 |
| A6 | 点 `改名称` 按钮 | **弹出改名输入框**（本轮修复：此前点了没反应） | 弹窗是否出现 |
| A7 | 输入新名字 → 点 `ConfirmButton` | 名称更新 + Toast「昵称已更新（本地模拟）」+ 弹窗关闭 | 名称是否变化 |
| A8 | 再点 `改名称` → 点 `CancelButton` | 弹窗关闭、名称不变 | 弹窗是否关闭 |
| A9 | 输入空 → 点确认 | Toast「昵称不能为空」、弹窗不关 | 有无提示 |
| A10 | 切到「主页展示」页签 | 显示 **1 张卡**；点击进"主页展示"页 | 有无卡 |

## B. 大厅（HomePage）

| # | 步骤 | 预期 | 失败请回传 |
|---|---|---|---|
| B1 | 看**邮件入口**（`BottomFunctionIcon_Mail`） | **有未读/未领邮件时出现红点**（本轮新建） | 有无红点 + 邮件页里的未读状态 |
| B2 | 点邮件入口 | 进邮箱页 | — |
| B3 | 点 `ActivityButton` | 进活动页 | — |
| B4 | 点其余入口（商店/排行/扭蛋/弹珠/背包/头像…） | 已接入的进页面；未接入的给"功能开发中"提示（**不再是死按钮**） | 哪个入口点了完全没反应 |

## C. 邮箱（MailPage）

| # | 步骤 | 预期 | 失败请回传 |
|---|---|---|---|
| C1 | 看邮件列表 | 显示 **2 封**（本地模拟数据），列表项有名称/日期/附件 | 条数 |
| C2 | 点其中一封 | 该封变为已读（名称前 `●` 消失） | 有无变化 |
| C3 | 点 `All read button`（一键） | 附件入包 + Toast「已批量领取 N 封邮件附件」（本轮修复：此前点了没反应） | 有无反应 |
| C4 | 看 Console | **不应有**新的 Exception/Error | 原文 |

## D. 弹窗与遮罩（本轮重点）

| # | 步骤 | 预期 | 失败请回传 |
|---|---|---|---|
| D1 | 打开任一弹窗（如商店购买界面） | 弹窗**后面有半透明黑色遮罩**（本轮新建） | 有无遮罩 |
| D2 | 弹窗打开时点遮罩区域/下层页面 | **点不动**（输入被阻断） | 能否点到下层 |
| D3 | 关闭弹窗 | 遮罩**随之消失**（不残留） | 是否残留 |
| D4 | 商店：点道具卡 → 弹购买界面 → 点 `取消键Button` | 购买界面关闭、可继续操作 | — |
| D5 | 商店：完成一次购买 | 弹 `Purchase Success Page`，**点任意处关闭** | — |

## E. 排行榜阵容弹窗（本轮重点，⚠️ 请特别留意）

| # | 步骤 | 预期 | 失败请回传 |
|---|---|---|---|
| E1 | 进排行榜 → 点某行的「查看」 | 弹出 `Player lineup`，显示**层数/回合数/战力** | 数字有无 |
| E2 | 看阵容卡区域 | 显示**卡牌**（名称/等级/星级/立绘） | **有没有卡** + 截图 |
| E3 | 点右上角 `关闭Button` | 弹窗关闭（本轮新建） | 按钮有无/能否关 |
| E4 | 打开弹窗时观察 | 不应把整个卡牌区隐藏掉 | 是否空白 |

> ⚠️ 已知情况：阵容弹窗里**本来就有 6 张 `角色卡牌Button-final`**（既有列表），本轮我又新建了 3 张 `展示卡牌1..3`；
> 代码现在**两种名字都收**（并排除名为 `展示卡牌Scroll View` 的滚动容器，避免误关整个列表）。
> 请重点看 **E2/E4**：若显示正常，我会把多余的 3 张清掉（已记为待核验清理项）。

## F. 英雄 / 图鉴 / 详情 / 编队

| # | 步骤 | 预期 | 失败请回传 |
|---|---|---|---|
| F1 | 进英雄页 | 显示 **46 张卡**（49 − 3 模拟未拥有），进度 **46/49** | 条数 |
| F2 | 切「图鉴」页签 | 显示全部 **49** 张（含锁/遮罩） | 条数 |
| F3 | 点一张卡 → 英雄详情 | 详情打开；底部三页签（天赋/秘技/终结技）可切 | 页签能否切 |
| F4 | 详情页看说明文本 | 显示分档后的数值（如 `8%` 变成对应档位），**无异常** | Console 原文 |
| F5 | 进编队页 | 卡片可点、3 槽位/4 队伍可切 | — |

## G. 活动 / 全图鉴活动

| # | 步骤 | 预期 | 失败请回传 |
|---|---|---|---|
| G1 | 活动页 | 新手 **5 条** / 进阶 **5 条** / 赛季 **6 个**；三页签可切 | 条数 |
| G2 | 点一条已完成的活动的领取 | 状态变化 + 奖励入包 | — |
| G3 | 全图鉴活动页 | 可进入、可返回 | — |

---

## 已知未完成 / 待你决定（**不算缺陷**，除非你另有要求）

| 项 | 状态 |
|---|---|
| **个性化「确定键」按钮**（6 处） | 无绑定，**保留待确认**：本实现是"点条目即装备"，该按钮要不要、语义是什么属产品规则 |
| 邮箱 `All delete`（全部删除） | **未实现**：数据层没有任何删除/批量已读接口 |
| 元素筛选按钮（英雄页/编队页的 光/水/土/风/火/暗） | 你此前明确**本阶段不做** |
| 缺图资源（头像 `HeroAvatar_*`、头像框、徽章、属性图标、道具图标、元素 6=暗） | **等美术补图**；缺的显示为空/模板图，不报错 |
| `Email Details Page（Have）/(No)`、`Success Receipt Interface` 等 | **未接入**（页面存在但无运行时代码） |
| `SystemLayer` 分层（`RISK-020`） | 等你选 A/B/C |
| 邮件正文/领取状态无文本节点（`BUG-024`） | 等你选 A/B/C |
| 战力展示口径（`玩家战力=100` vs `队伍战力=0`） | 等你确认 |
| 装备数据来源（配置表无"装备"类型） | 等你确认 |

---

## 已自动验证的基线（供你对照，不能替代人工）

> ✅ **本清单里的预期数字已逐条与代码/基线核对过**（2026-09-21）：
> A3 收藏品 `1/1/1/3/1`（`Logs/b1-prebuild-baseline.log`）· F1 英雄页 **46 张 / 46·49**（`LocalClientDataService.SimulatedLockedHeroCount = 3` ⇒ 49−3=46；进度 = `已收集 + "/" + 图鉴总数`）
> · F2 图鉴 **49 张** · C1 邮件 **2 封** · G1 活动任务 **新手 5 / 进阶 5**、赛季 **6** · B1 红点条件 = "存在未读**或**未领邮件"，
> 而模拟数据两封邮件的 `IsRead=false, IsClaimed=false` ⇒ **红点应当亮**。
>
> 💡 **实操提示**：A4（未拥有→Toast）可以直接用**头像框**列表测 —— 它表内可见 **3** 个但只拥有 **2** 个 ⇒ 里面必有一个"未解锁"条目 ✓

| 项 | 结果 | 证据 |
|---|---|---|
| 编译（Editor / Editor-asm / WebGL profile） | **0 error** | `Logs/verify-compile-*.log` |
| 纯逻辑 + **场景接线**自检 | **96 项 / 失败 0**，退出码 0 | `Logs/self-test3.log` |
| 表数据 | 10 张可用、`Hero 54 行（可见 49）`；收藏品可见 **1/1/1/3/1** | `Logs/b1-prebuild-baseline.log` |
| 节点契约（代码找但场景/prefab 都没有） | **无未处理真缺口** | `Logs/ui-contract-audit.md` |
| 字段类型一致性（TMP vs 旧版 Text） | **0 处不符**（带阳性对照） | `Logs/ui-type-audit.md` |
| 返回·关闭闭环 | 完整（各页 `BackoffButton` / `后退Button` 均已绑） | `Logs/ui-flow-matrix.md` |
| 死按钮 | 169 个按钮 → 5 处真缺口，**4 处已修**（第 5 处待你确认） | `Logs/ui-dead-buttons.md` |

## 复现命令（如果你想自己跑）

```powershell
# 编译三目标
& 'D:\unity project\pinball\Logs\verify-compile.ps1' -Profile All

# 纯逻辑 + 场景接线自检（失败会以非 0 退出码结束）
$env:ALLUSERSPROFILE = 'C:\ProgramData'
& 'C:\Program Files\Unity\Hub\Editor\2022.3.57f1c2\Editor\Unity.exe' -batchmode -quit `
  -projectPath 'D:\unity project\pinball' `
  -executeMethod 'Pinball.Client.Editor.ClientSelfTest.RunAll' `
  -logFile 'D:\unity project\pinball\Logs\self-test.log'
```

> ⚠️ 跑批处理前**必须先关掉已打开的 Unity 编辑器**（否则会因"工程被占用"而崩溃，退出码 `0x40000015`）。
