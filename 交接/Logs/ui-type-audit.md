# UI type audit (declared field type vs referenced component type)

Rule: AGENTS.md sec.4 - Text references must match the real component type (TMP vs legacy UGUI Text).
A mismatch silently nullifies the reference (root cause pattern of BUG-018).

references checked = 127

## Mismatches

(none)

