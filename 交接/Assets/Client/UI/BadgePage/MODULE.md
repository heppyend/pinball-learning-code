# Client BadgePage 模块说明

## 职责

个人中心的铭牌选择页。页面位于 `ClientCanvas/PagesLayer/BadgePage`，展示负责人提供的拆分铭牌素材，并通过本地数据服务保存当前使用的铭牌。

## 素材

`Sprites/` 原样复制自负责人提供的 `UI/铭牌`。`参考.png` 仅用于布局核对，禁止作为整页运行时背景。

## 限制

当前仅提供本地模拟的三张可选铭牌和确认状态；真实解锁条件、服务器同步、头像/头像框/称号页签待确认。
