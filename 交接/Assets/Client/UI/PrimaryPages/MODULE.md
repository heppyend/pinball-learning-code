# Client PrimaryPages 模块说明

## 职责

存放主页尚未深入开发的一级页面场景节点：扭蛋、商城、邮箱、排行榜、活动、任务和公告。每个页面是 `ClientShell` 内独立根节点，可独立调整，统一只完成入口、基础状态、返回闭环。

## 限制

未确认的卡池、商品、邮件、榜单、活动、任务和公告数据不在 UI 回调中硬编码，也不接入真实服务端；页面仅标注为待接入。

## 验证

从主页逐一点击所有一级入口，应进入对应名称的独立页面并能返回主页。
