# CSharp 模块说明

## 职责

存放热更新资源加载、构建辅助以及与现有热更新流程相关的 C# 代码；其中包含 `HotUpdate` 与 `MyBuild` 子目录。

## 约束与验证

- DLL、资源包、加载地址和初始化顺序属于兼容性边界，修改前先阅读 `Assets/Main/MODULE.md`。
- 变更后至少验证程序集编译、热更新资源生成和启动加载；未验证目标平台时不得声称线上可用。
