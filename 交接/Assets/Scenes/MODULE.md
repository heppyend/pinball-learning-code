# Scenes 模块说明

## 职责

存放运行时场景、测试场景以及场景配套的特效预制体。构建设置当前仅显式启用 `Assets/Main/Boot.unity`；其他场景可能通过 YooAsset 地址异步加载。

## 约束与验证

- 不因场景未出现在 Build Settings 就删除；先核验 YooAsset 清单、代码字符串引用、预制体依赖和运行路径。
- 调整场景、特效或预制体时保持 `.meta`/GUID，不直接修改第三方示例场景。
- 删除候选先记录在根目录 `BUG_TRACKER.md`，并完成目标模式下的资源加载验证后再执行。
